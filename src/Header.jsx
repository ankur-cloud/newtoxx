import React from 'react'
import { Toolbar } from '@material-ui/core';

const Header = () => {
    return (
        <div>
            <Toolbar className='title'> List Items</Toolbar>
        </div>
    )
}

export default Header
